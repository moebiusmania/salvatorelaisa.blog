{
  "name": "ghost-now",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": "6.11.x"
  },
  "scripts": {
    "start": "ghost start",
    "postinstall": "node index.js && ghost install local"
  },
  "dependencies": {
    "ghost-cli": "^1.0.0"
  }
}
